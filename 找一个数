题目描述
找到一个重复奇数次的数字，然后输出数字。

例1：

如果输入为：12 12 12 12 15

那么输出是：15

例2：

如果输入是：12 13 12 13 18 12 13 13 18

那么输出是：12


输入
第一行包含正整数N <500000，然后，N个正整数跟随（用空格分隔），每个小于1 000 000。


输出
在输入序列中，只有一个数字X重复奇数次。其他人甚至出现过多次。你应该输出X.


样例输入
9
3 1 2 2 17 1 3 17 3

样例输出
3

提示
如果你可以避免错误“Memory Limit Exceed”，这个问题将是一个非常简单的问题。

#include <stdio.h>
int main()
{
    int i,sum,n,a;
    sum=0;
    scanf ("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf ("%d",&a);
        sum^=a;
    }
    printf ("%d",sum);
    return 0;
